<h1>All Articles</h1>
<p>they're hare-raising!</p>

<ul class='articles-list'>
	<%	@articles.each do |article| %>
		<li id=<%="article-" + "#{article.id}" %> >
			<h3> <%= link_to(article.title, article_path(article.id)) %> </h3>
			<p> <%= article.shorten %> </p>
		</li>
	<% end %>
</ul>